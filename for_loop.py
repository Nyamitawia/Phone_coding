# Create a list of numbers
numbers = [1, 5, 3, 9]

# Initialize a variable to store the total sum
total = 0

# Use a for loop to iterate over the numbers list
for number in numbers:
    total += number  # Add the current number to total

# After the loop, print the final total
print("The sum of all the numbers is:", total)
